export const createDeployment = async (): Promise<void> => {
    // const deployment = await github.rest.repos.createDeployment({
    //     owner: github.context.repo.owner,
    //     repo: github.context.repo.repo,
    //     ref: github.context.sha,
    // });
};
